#pragma once

struct Position
{
    double x = 0.0; // meters
    double y = 0.0; // meters
    double z = 0.0; // meters
};

struct Velocity
{
    double x = 0.0; // meters per second
    double y = 0.0; // meters per second
    double z = 0.0; // meters per second
};

